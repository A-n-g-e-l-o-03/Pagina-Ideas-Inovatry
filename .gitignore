# Local webhook config overrides â€” NEVER commit a real Discord webhook token.
# The committed js/form-config.json keeps a placeholder; the real URL is
# injected at deploy time (see README). Local overrides stay local.
**/js/form-config.local.json

# OS / editor junk
Thumbs.db
.DS_Store
# Dependencies
**/node_modules/

# Build outputs
**/playwright-report/
**/test-results/
**/coverage/
**/.cache/
**/dist/
**/.tmp/

# Env
**/.env
**/.env.local

# OS (duplicated removed)

